/// App single-user (CLAUDE.md Section 1) — satu UUID tetap dipakai sebagai
/// `user_id` di semua tabel lokal, supaya konsisten dengan `user_id` yang
/// sama dipakai backend saat sync nanti (Phase 7).
const String kLocalUserId = '0e743f95-0eae-4fee-b327-283bd2b7ecae';
